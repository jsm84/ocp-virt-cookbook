apiVersion: cdi.kubevirt.io/v1beta1
kind: CDI
metadata:
  name: cdi-kubevirt-hyperconverged
spec:
  config:
    podResourceRequirements:
      limits:
        cpu: "2"
        memory: 1Gi
      requests:
        cpu: 100m
        memory: 60Mi
    filesystemOverhead:
      global: "0.055"
