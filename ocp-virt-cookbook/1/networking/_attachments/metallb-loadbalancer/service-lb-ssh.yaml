apiVersion: v1
kind: Service
metadata:
  name: web-vm-ssh
  namespace: metallb-demo
spec:
  type: LoadBalancer
  selector:
    app: web-vm
  ports:
    - name: ssh
      protocol: TCP
      port: 22
      targetPort: 22
