apiVersion: v1
kind: Service
metadata:
  name: web-vm-lb
  namespace: metallb-demo
spec:
  type: LoadBalancer
  selector:
    app: web-vm
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
